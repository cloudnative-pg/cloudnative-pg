project_name: kubectl-cnp

release:
  github:
    owner: EnterpriseDB
    name: kubectl-cnp

env:
  - GO111MODULE=on
  - CGO_ENABLED=0

before:
  hooks:
    - go mod download

builds:
- id: kubectl-cnp
  binary: kubectl-cnp
  main: cmd/kubectl-cnp/main.go
  gcflags:
  - all=-trimpath={{.Env.GOPATH}};{{.Env.PWD}}
  ldflags:
  - -s
  - -w
  - -X github.com/cloudnative-pg/cloudnative-pg/pkg/versions.buildVersion={{.Version}}
  - -X github.com/cloudnative-pg/cloudnative-pg/pkg/versions.buildCommit={{.ShortCommit}}
  - -X github.com/cloudnative-pg/cloudnative-pg/pkg/versions.buildDate={{.CommitDate}}
  goos:
  - darwin
  - linux
  - windows
  goarch:
  - amd64
  - 386
  - arm64
  - arm
  - ppc64le
  - s390x
  goarm:
  - 5
  - 6
  - 7
  ignore:
  - goos: darwin
    goarch: 386
  - goos: windows
    goarch: ppc64le
  - goos: windows
    goarch: s390x

archives:
- replacements:
    386: i386
    amd64: x86_64

checksum:
  name_template: '{{ .ProjectName }}-{{ .Version }}-checksums.txt'

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  skip: true

signs:
  - artifacts: checksum
    args: ["--batch", "-u", "{{ .Env.GPG_FINGERPRINT }}", "--output", "${signature}", "--detach-sign", "${artifact}"]
