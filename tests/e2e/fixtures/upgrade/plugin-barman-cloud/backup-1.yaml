apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: cluster-backup
spec:
  target: primary
  cluster:
    name: cluster1
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
