apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: backup-plugin-minio
spec:
  method: plugin
  cluster:
    name: pg-backup-plugin-minio
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
